public boolean reverse() {

    if (accountUsed == null) {
        return false;
    }

    accountUsed.deposit(amount);

    System.out.println(
            "Withdrawal reversed successfully.");

    return true;
}
